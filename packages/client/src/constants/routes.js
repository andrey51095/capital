const root = '/';
const capital = '/capital';
const investmentCalculator = '/investment-calculator';
const expenses = '/expenses';
const income = '/income';
const login = '/login';

const routes = {
  root,
  capital,
  investmentCalculator,
  expenses,
  income,
  login,
};
export default routes;
